# API backend (обязательно в production на Vercel)
# Локально без .env используется http://localhost:8000
VITE_API_URL=http://localhost:8000

# Ссылка для ученика (опционально). Если не задано — берётся {текущий origin}/student
# VITE_STUDENT_URL=https://your-app.vercel.app/student
